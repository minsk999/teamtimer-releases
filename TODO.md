# 다음 작업 (Claude Code)

> 배경은 `STATUS.md`, 프로젝트 규칙은 `CLAUDE.md`.
> **작성 시점: 2026-08-28 밤. 다음 세션은 회사 PC에서 이어받는다.**

## 이어받는 세션이 가장 먼저 할 일

1. `STATUS.md` 를 읽는다 (오늘 한 일 전부)
2. `CLAUDE.md` 의 **「자동 업데이트」 절**과 **macOS 규칙 ④** 를 읽는다 — 오늘 실측으로 밟은 함정들이다
3. 아래 **A) 사용자 확인 대기** 를 먼저 물어본다. 이게 풀려야 B 가 움직인다

---

## A) 사용자 확인 대기 — 코드로는 더 못 간다

### A-1. GAS 진단 스크립트 실행 ★가장 급함

딸깍 세션이 만들어 사용자에게 전달했다. **읽기 전용, 시트를 안 건드린다.**

```
파일: C:\Users\misk9\ClaudeCode\intranet-ttalkak\server\진단_붙여넣기용.gs
방법: 구글 시트 → 확장 프로그램 → Apps Script → 기존 코드 **맨 아래에 추가**
     → 함수 드롭다운에서 runAllDiagnostics 선택 → ▶ 실행 → 실행 로그 전체 복사
```

이 로그 하나로 다섯 가지가 한 번에 갈린다:
- **이름 오탐** — 작업이 조용히 사라지거나 남의 것으로 보이는가 (가장 중요)
- **박지수 딸깍 전송 불가** (A-2)
- 관리항목 추가 시 행 밀림 / idx 커버리지 / ▼업무 구분선 위치

### A-2. 박지수 님 딸깍 증상 (3갈래 반증 테스트)

박지수는 시트 맨 아래 이름행(75)이라 그 밑에 경계가 없다. insert 인라인 루프의 기본값
`nameSheetRow + 100` 이 살아남아 `allData[173]` 을 읽는데 `READ_ROWS = 150` → **TypeError**.
**읽기 경로는 클램프돼서 멀쩡하다** — 타이머만 봐선 절대 안 보인다.

| 증언 | 판정 |
|---|---|
| 항상 안 된다 | 가설 일치 → v55 배포로 해결 |
| 잘 된다 | **기각** — A/I/J 열에 경계가 있다는 뜻 |
| 될 때도 안 될 때도 | **기각** — 지금 조건에선 결정적이라 간헐일 수 없다 |

### A-3. 시트 좌측(A~G) 10~24행 확인

구민석 구역만 9행에서 끊겨 보인다. **비어 있으면 정상**(빈 행은 응답에서 빠진다).
딸깍 세션 해석(그냥 빈 행)이 더 유력하고, 진단 ③번이 확정한다.

---

## B) 서버 (딸깍 세션이 주도, 타이머는 검토·동기화)

### B-1. GAS v55 배포 — 준비 완료, 사용자 배포만 남음

`intranet-ttalkak/server/GoogleAppsScript.v55.gs` (전체 붙여넣기용 완성본).
**타이머 코드 변경 불필요.** 들어가는 것:

| | |
|---|---|
| A | `scanLen < 1` 가드 |
| B | action 화이트리스트 (`{ok:false, error:"알 수 없는 action"}` 로 응답) |
| C | `member` 파라미터 통일 (하위호환 추가만) |
| D | `READ_ROWS` 클램프 |
| E | **`readGrid` 경유** — `addMemo`(:659)·insert(:1062) 포함 ★ |
| F | 경계 기본값 치환 (`:1076`) — **진단 후** 조건부 |

**E 가 중요하다.** v54 의 READ_ROWS 수정이 **쓰기 경로엔 안 들어갔다**.
`readGrid` 를 거치는 경로에서만 참이라, 시트가 **130행을 넘는 순간** 150행 절단이
쓰기 쪽에서 되살아난다. 현재 마지막 항목 93행 — **여유 37행.**

배포되면 `teamtimer/gas/` 사본을 v55 로 맞추고 `gas/README.md` 버전 표기도 갱신할 것.

### B-2. GAS v56 — 설계 합의 완료, 서버 미구현

전문: **`gas/v56-expectTitle-초안.md`**. 요약:
- `deleteTask` = `expectIdx` + `expectTitle` **둘 다** (실패 조건이 서로 반대)
- 나머지 3종 = `expectTitle` 단독
- `LockService`: **읽기에 걸지 말 것** / 8초 / `{ok:false, busy:true}`
- 필수화(4단계)는 `expectIdx` 만

**타이머 쪽은 이미 구현 끝났다** — 지문 발신 + `stale` 처리 + `busy` 1회 재시도.
서버가 올라오면 바로 동작한다.

⚠️ **선행 조건: 이름 오탐 수정이 먼저다.** 오탐 상태에서 지문을 넣으면 정상 동작까지
전부 stale 로 막히고, 원인을 지문 쪽에서 찾게 된다.

### B-3. 딸깍 세션과의 연락

세션 간 메시지로 조율해 왔다. 상대 세션이 살아 있으면 `ListAgents` 로 찾을 수 있다.
**서버 수정은 상대에게 먼저 알리고 합의한 뒤 사용자에게 올린다**(합의된 규칙).

---

## C) 앱 디벨롭 — 시안 확인 완료, 착수 대기

5관점(UX/UI·모션·개발·기획·디렉터) 분석 → 시안 전달까지 끝났다.
**시안: `_audit/앱-디벨롭-시안.html`** (gitignore, 브라우저로 열면 된다)

> 진단 한 줄: 이 앱은 **"흘끗 보면 지금 뭐가 급한지 안다"** 가 존재 이유인데
> 그 한 줄이 세 군데서 새고 있다. 새 기능이 아니라 **이미 손에 쥔 데이터와
> 이미 있는 공간을 화면에 되돌려 놓는** 사이클이다.

| # | 항목 | 영역 | 비용 |
|---|---|---|---|
| 1 | **카드 폭을 제목에 돌려주기** — 액션 hover 노출 + 알람 칩 복구 | 작업카드 | 작음 |
| 2 | **마감 임박도를 색 하나에서 꺼내기** — '지남' 상태 + 라이트 대비 보정 | 타이머 | 작음 |
| 3 | **완료 상태·메모를 화면에 되돌려 놓기** — 빈 78px 채우기 | 완료처리 | 작음 |
| 4 | **동기화 표시등이 실패해도 초록** — 신선도 반영 + 오프라인 캐시 | 상단바 | 보통 |
| 5 | **마감 알림이 새어 나감** — 딸깍 유입엔 알람이 안 붙는다 | 알림 | 보통 |

**1~3 은 비용이 작아 묶어서 한 번에 가도 된다.** 사용자가 어느 것부터 할지 정할 차례.

각 항목의 근거·정확한 변경안·시안에서 봐야 할 것은 시안 HTML 안에 다 적혀 있다.
원본 스펙은 워크플로 결과에 있다(세션이 끊기면 재실행 필요).

### 시안 없이 바로 해도 되는 11건 (quick wins)

명백한 버그성이거나 되돌리기 쉬운 것. 시안 HTML 맨 아래 목록 참조. 특히:

- **실패 토스트에 체크(✓) 아이콘이 붙는다** — 성공/실패가 픽셀 단위로 같다. 실패 문구 30곳 이상
- **설정 [초기화]가 확인 없이 즉시 실행** — 다른 모달은 그 자리가 [취소]다(오클릭 위험)
- **메일은 10초, 작업 목록은 10분** — 딸깍이 보낸 게 배정 메일로 먼저 온다. 메일이 새 요청을
  감지하면 `doSync` 도 같이 걸면 된다
- 메일 폴링이 안 보는 화면을 10초마다 다시 그리고 최대 150KB 를 동기 기록
- 완료 프리셋이 방금 입력한 상태 메모를 조용히 버린다
- FLIP 이 인라인 transition 을 남겨 이동한 카드의 hover 가 그때부터 망가진다
- 창 크기를 기억하지 않는다
- 완료 체크에 눌림 피드백이 없다
- reduced-motion 대응이 비대칭(정작 모달·오버레이가 빠짐)
- 쓰지 않는 프로토타입 경로(`confirmImport`)가 가짜 카드를 목록에 넣는다
- 매초 카운트다운이 스크롤바 MutationObserver 를 깨워 강제 레이아웃

---

## D) 미해결 · 장기

### D-1. macOS 자기 교체 — App Management TCC 실측 (맥 필요)

`MAC_AUTO_APPLY = false` 로 잠겨 있다. **코드를 더 쓰기 전에 실측이 먼저다.**

```bash
# 앱을 켜 둔 채로, 터미널과 앱 자신에서 각각
mv /Applications/TeamTimer.app /Applications/.tt-bak.app
```
`Operation not permitted` → Developer ID($99) 필요. 아니면 플래그를 켜고 P4~P9(교체·셀프테스트·롤백) 추가.

### D-2. macOS 실기 확인

v1.0.27~1.0.30 이 **맥에서 한 번도 실행 검증되지 않았다.** 특히 v1.0.29 가
**처음으로 제대로 된 x64 자산**이라 Intel 맥에서 확인이 필요하다.

### D-3. 업데이트 캐시가 안 지워진다

설치 후 `%LOCALAPPDATA%\team-timer-updater\` 에 **157MB**가 남는다(같은 파일 두 벌).
electron-updater 가 다음 업데이트 때 덮어쓰지만 정리 로직을 넣는 게 맞다.

### D-4. 앱을 껐다 켜면 받아둔 것이 초기화된다

파일은 위 폴더에 그대로 있는데 앱이 "받아뒀다"를 기억 못 해 [받기]부터 다시 해야 한다.
`euDownloaded` 가 프로세스 메모리라서다.

### D-5. 이전 감사에서 미룬 것

- Apps Script 공유 토큰(`SHEET_TOKEN`)
- `_apptest/`·`_mintest/` 삭제 (⚠️ `_apptest` 는 앱 전체의 낡은 포크다. `CLAUDE.md` 경고 참조)
- 마우스 버튼 4/5 주석↔코드 불일치
- 백드롭 클릭이 `closeModal` 을 안 거쳐 퇴장 모션을 건너뛴다
- 설정 `[저장]/[초기화]` 가 스크롤 영역 안에 있다(헤더만 고정됨)
