# 현재 상황

> 작업일 **2026-08-28** · 이 문서 정리 **2026-09-01**
> 다음 세션은 회사 PC에서 이어받는다. **`TODO.md` 맨 위부터 읽을 것.**

## 한 줄 요약
**v1.0.30 배포·승격 완료.** 자동 업데이트를 만들어 **실전 완주까지 확인**했고(1.0.28→1.0.29 실기),
그 과정에서 **v1.0.12부터 16개 버전이 맥 x64 자산에 arm64 바이너리를 넣고 있던 것**을 잡았다.
업데이트 배너는 4관점 분석 → 시안 확인 → 적용 → 26에이전트 회귀검증까지 한 바퀴 돌았다.

## 버전 현황
| 항목 | 버전 | 비고 |
|---|---|---|
| 타이머 앱 | **v1.0.30** (정식, latest) | 자동 업데이트 탑재 |
| GAS | **v54** (배포본) | v55 는 딸깍 세션이 완성, **배포 대기** |
| 인트라넷 딸깍 | v1.5 | |
| 설명서 | v1.0.30 반영 완료 | |

⚠️ **팀원은 대부분 아직 v1.0.27 이하다.** v1.0.27 에는 자동 업데이트가 **없어서**,
배너를 눌러도 설명서 페이지가 열릴 뿐이다. **v1.0.28 이상을 한 번은 직접 설치**해야
그 다음부터 앱 안에서 자동으로 받는다. 팀 공지 시 이 점을 알릴 것.

---

## 오늘 한 일

### 1. 자동 업데이트 (v1.0.28 신규 → v1.0.29 로 실전 검증)

**Windows** = electron-updater(NSIS). **macOS** = 자체 구현(릴리스 API 직접 조회 →
sha256 검증 → `ditto -x -k` 전개 → `codesign --verify` → Finder 안내).
맥의 **자기 교체는 `MAC_AUTO_APPLY = false` 로 잠겨 있다**(App Management TCC 실측 전).

**실전 완주 확인(2026-08-28 02:27, 본인 PC):**
```
v1.0.28 설치본 → 배너 감지 → 앱 안에서 다운로드 → sha512 검증
→ 조용히 설치(마법사 안 뜸) → 자동 재시작 → v1.0.29
```

구현 중 실측으로 밟은 함정은 **`CLAUDE.md` 의 「자동 업데이트」 절**에 전부 적어 뒀다.
특히 `checkForUpdates()` 누락 건은 **겉보기엔 멀쩡한데 [받기] 가 항상 실패**하는 종류라
34에이전트 리뷰에서 blocker 3건이 같은 원인으로 수렴했다.

### 2. ★맥 x64 자산이 v1.0.12부터 arm64였다

릴리스 후 자산을 검사하다 **x64 zip 과 arm64 zip 의 sha256 이 완전히 동일**한 것을 발견.
받아서 Mach-O 헤더를 읽으니 `cputype = 0x0100000C`(arm64)였다. **DMG 도 같았다.**

원인은 `build.mac.target` 의 `arch:["x64","arm64"]`. `build:mac` 은 `--prepackaged` 로
앱을 하나씩 지정해 두 번 도는데, arch 배열이 있으면 **매 호출이 x64·arm64 자산을 둘 다**
그 하나의 앱으로 만든다 → 마지막(arm64) 호출이 x64 이름 자산까지 덮어쓴다.

- `package.json`: `"target": ["dmg","zip"]` 로. CLI 의 `--x64`/`--arm64` 가 결정한다
- `release.yml`: **`Verify mac artifact architectures`** 단계 추가. 이름↔실제(`lipo -archs`)가
  다르면 빌드 실패. zip 은 `ditto` 전개, dmg 는 `hdiutil` 마운트해서 본다
- v1.0.29·v1.0.30 빌드에서 4자산 전부 OK 확인

> 팀에 Intel 맥 사용자가 있었다면 그동안 맥 설치가 안 됐을 것이다. **v1.0.29 가 처음으로 제대로 된 x64 자산**이다.

### 3. 업데이트 배너 개편 (4관점 → 시안 → 적용 → 26에이전트 회귀검증)

사용자 제보: *"생기고 사라질 때 모션이 툭툭 끊긴다"*

**원인은 이징이 아니라 구조였다.** 배너가 `.app` flex 열의 첫 자식이면서
`display:none↔flex` 라, `.show` 가 붙는 **한 프레임에 45px 이 삽입**돼 타이틀바·목록이
0ms 하드컷으로 내려앉고 **그제서야** 배너가 400ms 슬라이드했다. 퇴장은 거울상.

→ 래퍼의 `grid-template-rows: 0fr↔1fr`. **자리가 열리는 속도 = 배너가 내려오는 속도.**
실측으로 타이틀바가 정확히 45px 를 같은 곡선으로 따라온다.

⚠️ **그리드 아이템에 패딩을 두면 안 된다.** `0fr` = `minmax(auto,0fr)` 이라 트랙 최소값이
min-content 인데 패딩은 `min-height:0` 으로 안 줄어 18px 이 남는다(실측). `.ub-row` 를
한 겹 두고 패딩을 그 안으로 옮겼다.

**같이 잡은 것:**
- 다크에서 배너가 안 보였다 — `#111` 고정인데 다크 `--bg` 가 `#0a0a0a`(채널당 7/255 차)
  → `var(--pill)`/`var(--pill-text)` 로 두 테마 반전
- 받아둔 상태가 메인 창에 안 보였다(사용자 제보) — 배지가 설정 모달 안에만 있었다
  → ready 가 되면 `[다음에]` 억제를 풀고 배너를 다시 띄운다
- **배지·배너가 세션 내내 죽을 수 있었다** — win 은 `update-downloaded` 가 유일한 출구인데
  유실되면 `downloading` 에 영구 고정 + '받는 중 무시' 가드에 걸려 무반응. 90초 워치독 추가

**회귀검증(26에이전트)에서 제가 넣은 것이 문제로 잡혔다:**
- `.no-later`(ready 에서 [다음에] 숨기기)가 **두 문제를 동시에** 만들었다 —
  [다음에] 는 테두리가 있어 26→28px 이라 배너가 **전이 없이 46→44px 로 튀고**,
  ready 배너를 **닫을 방법이 사라졌다**. 폐기. 6가지 상태 전부 46px 실측 확인
- 워치독이 **되지도 않는 재시도를 권했다** — electron-updater 는 `downloadPromise` 가
  살아 있으면 멈춘 그 promise 를 그대로 돌려준다. `stalled` 상태를 새로 두고
  "앱을 껐다 켜 주세요" 로 바꿈
- 트레이 '업데이트 확인' 이 [다음에] 이후 **완전 무반응**이었다 → `pushUpdateCheck(force)`

### 4. 설정창에서 확인 팝업이 뒤에 가려지던 것 (사용자 제보)

오버레이가 전부 `z-index:100` 이라 **DOM 순서로 겹침이 정해지는데**, `#modal-settings` 가
가장 뒤에 있어 그 위에 열리는 모달이 통째로 가려졌다. 사용자에겐 **"클릭해도 무반응"** 으로 보인다.
`#modal-keys` 가 이미 같은 이유로 `z-index:120` 을 갖고 있었고 `#modal-confirm` 만 빠져 있었다.

### 5. 완료 처리에 '베리 전달 완료' 추가

`수정 전달 완료` ↔ `담당 제작 완료` 사이. 프리셋이 4개가 되면서 440px 창에서 3+1 로 흐른다
(사용자가 "3+1도 이상하지 않다"고 확인 — 강제 2+2 는 넣었다가 뺐다).

### 6. 딸깍 세션과 서버 계약 조율 (진행 중)

작업타이머와 인트라넷 딸깍이 **같은 GAS 웹앱·같은 시트**를 쓴다. 세션 간 메시지로 합의한 것:

- **정본 = `intranet-ttalkak/server/`**, 타이머의 `gas/` 는 참조 사본(`gas/README.md` 배너 참조)
- **행 키 지문 검사(v56)** 설계 합의 완료 — 전문은 `gas/v56-expectTitle-초안.md`
- **LockService**: 읽기에 걸지 않기 / 8초 / `{ok:false, busy:true}` 로 구분
- 타이머 쪽 클라이언트는 **이미 구현 끝**(지문 발신 + `stale` 처리 + `busy` 재시도)

---

## 실측으로 확보한 시트 데이터 (2026-08-28)

`?action=read` 응답을 직접 분석한 결과. **다음 세션에서 재측정 전에 참고만 할 것.**

```
좌: 구민석 3~9(nameRow 2)   주지현 25~46(24)   김본희 49~68(48)   박지수 76~93(75)
우: 한영채 3~18(2)          박나진 25~40(24)   구정현 49~66(48)
영상작업 88건 · idx 추출 100% · 같은 팀원 칸 안 제목/idx 중복 0건
```

⚠️ **`doRead` 응답만으로는 이름 오탐 부류가 안 보인다** — 오탐이 나면 원인이 된 그 행 자체가
응답에서 빠진다. raw 그리드를 직접 읽어야 한다(딸깍 세션 진단 스크립트).

---

## 릴리스 절차 (오늘 확립)

```bash
npm run bump 1.0.31        # package.json + docs/index.html 을 같이 고친다
git add -A && git commit -m "v1.0.31"
git tag v1.0.31 && git push origin main && git push origin v1.0.31
# CI 가 win+mac 빌드 → prerelease 로 올림 (팀에 알림 안 감)
gh release edit v1.0.31 --prerelease=false --latest    # 실기 확인 후 승격
```

- CI 에 **태그↔package.json 버전 일치 가드**가 있다. 어긋나면 빌드 실패
- CI 에 **맥 자산 아키텍처 검증**이 있다. 이름과 내용이 다르면 빌드 실패
- `latest-mac.yml` 은 릴리스에서 **제외**한다(맥은 Squirrel 을 안 쓴다)
- `on: release: [published]` 재발화가 승격한 릴리스를 다시 prerelease 로 되돌리던 것 → 차단됨
- 설명서는 **자산이 올라간 뒤** 커밋한다(먼저 올리면 다운로드 링크가 몇 분간 404)

---

## 열려 있는 것 → `TODO.md`
