# 현재 상황 (2026-08-27)

## 한 줄 요약
**v1.0.27 배포 완료.** 전면 감사(18에이전트·검증 통과 102건·critical 0)에서 나온 긴급 건 +
실사용 제보 3건 + 메일 종류별 알림 필터(TODO ①) + 설정창 정돈 + GAS v54 까지 한 세대로 나갔다.
감사 원본은 `_audit/`(gitignore).

## 버전 현황
| 항목 | 버전 |
|---|---|
| 타이머 앱 | **v1.0.27** (정식, latest) |
| GAS | **v54** |
| 인트라넷 딸깍 | v1.5 |
| 설명서 | v1.0.27 반영 완료 |

## v1.0.27 릴리스 기록 (2026-08-27)
- 빌드: win 1분11초 · mac 1분56초, **맥 서명 검증 통과**
- 자산 3종(exe·arm64 dmg·x64 dmg) + blockmap + `latest.yml`/`latest-mac.yml`
  ※ `--publish never` 인데도 `latest*.yml` 이 생성된다 — 감사 열린질문 #5 해소. 자동 업데이트 붙일 때 참고.
- prerelease 로 올려 본인 PC 확인 → `gh release edit v1.0.27 --prerelease=false --latest` 로 승격
- 승격 후 `releases/latest` = v1.0.27 확인, 앱 `cmpVer` 판정 재현으로 **1.0.26 → hasUpdate=true** 확인
- 설명서 다운로드 URL 3개 HTTP 200 확인 후 커밋(자산이 올라간 뒤에 올려야 404 를 피한다)

### 릴리스하며 함께 고친 것
- **`release.yml` 에 `prerelease` 키가 없었다**(감사 #12). 문서는 이 절차를 전제했는데 실제로는 없어서,
  태그를 미는 순간 검증 전 빌드가 latest 가 되고 팀 전원에게 배너가 뜰 뻔했다.
- `name: 작업 타이머 ${{ github.ref_name }}` — 예전엔 빌드 후 사람이 API 로 제목을 PATCH 했다.
- `retention-days: 1` — 기본 90일이면 아티팩트가 계정 단위 쿼터를 먹는다.
  예전에 이 저장소의 2GB 때문에 다른 저장소 맥 빌드가 업로드에서 막힌 적이 있다.

## 2026-08-27 수정 내역 (v1.0.27 로 배포됨)

| # | 무엇 | 어디 | 검증 |
|---|---|---|---|
| 1 | **알람이 내 보드 기준으로 판정**된다. 전엔 팀원 보드를 켜 두면 내 알람이 판정조차 안 돼 90초를 넘기면 영영 안 울렸고, 남의 마감 알림이 내 데스크톱에 떴다. `myTasks` 를 따로 유지하고 `firedAlarms` 키를 행번호→`timeKey` 로 바꿨다(좌/우 단이 행번호를 공유해 충돌했다) | `renderer` `mapVideoTasks`·`refreshMyTasks`·`checkAlarms` | 브라우저에서 실재현: 팀원 보드를 보는 중 내 알람만 발화·중복 없음·키 삭제 시 재발화 ✅ |
| 2 | **동기화 가드가 진행 중 Promise 를 돌려준다.** 전엔 `return;` 이라 `await doSync()` 가 아무것도 안 기다려, 방금 추가한 작업이 옛 응답에 덮여 사라지고 삭제 행번호 재조정이 무효였다. 겹친 호출이 오면 끝난 뒤 1회 더 돈다(상한 4) | `renderer` `doSync`/`runSyncOnce` | 겹친 호출 → 2회차 실행 확인 ✅ |
| 2b | 삭제 연타 잠금(`delInFlight`), 롤백 4곳을 `rollbackTasks` 로(배열이 갈아끼워졌으면 재동기화), 임시 작업 id 를 문자열→음수 | `renderer` | 문법·동작 확인 |
| 2c | `post-sheet` 20초 타임아웃. **타임아웃은 롤백하지 않는다** — GAS 는 응답만 늦고 쓰기는 커밋된 경우가 흔해 되돌리면 더 혼란스럽다. `{timeout:true}` 로 구분해 안내 후 재동기화 | `main.js` · `afterWriteFail` | — |
| 3 | **`_apptest/` 의 실서비스 웹앱 URL 제거** + `apptest.yml`/`mintest.yml` 에 `prerelease: true` | `_apptest/main.js` · 워크플로 2개 | — |
| 4 | **`card()` 에 `esc()`** (제목·요청글/기획안 href). `toast()` 와 공휴일 `title` 도 함께 | `renderer` | `A <b>test</b> B · 5 < 6` 이 그대로 표시되고 레이아웃 안 밀림 ✅ |
| 5 | `AppsScript_읽기API.gs` 상단에 **폐기 경고 배너** — 이 파일을 붙여넣으면 전원 동기화가 즉사한다(계약 불일치) | `.gs` · `CLAUDE.md` | — |
| 6 | **신규 팀원이 이름을 고를 수 있다.** `fetch-members` IPC 신설 → 설정 열 때 `?action=members` 로 명단 갱신. 전엔 명단 갱신이 `doSync` 안에만 있었고 `doSync` 는 이름이 없으면 조기 반환해서, 새 설치는 소스 폴백 7명만 봤다 | `main.js`·`preload.js`·`renderer` | 실제 웹앱 응답 확인: `{"ok":true,"members":[...],"source":"config"}` ✅ |

## 2026-08-27 추가 — 메일 종류별 알림 필터 (TODO ① 완료, v1.0.27 배포됨)

설정 → 알람 → **받을 메일 종류**. 체크칩 3개(업무 요청·코멘트 알림·담당자 배정) +
**목록에서도 숨기기** 토글(자동실행 행과 같은 `opt-row`+`mh-toggle`).
기본값 = 셋 다 켜짐 + 숨기기 끔(기존 동작 유지).
※ 처음엔 적용 범위를 seg 2버튼으로 냈다가 **직관적이지 않다는 지적으로 토글로 교체**했다. 되돌리지 말 것.
기존 `seg`/`seg-btn` 컴포넌트만 재사용했고 **새 CSS 는 만들지 않았다.**

- `MAIL_KINDS` 에 `key`(req/comment/assign) 추가 · `alarmSettings.mailKinds` + `mailFilterScope` 신설
- **어느 분류에도 안 걸리는 메일은 항상 통과** — 인트라넷이 제목 형식을 바꿔도 안 놓친다
- ★알림 게이트는 `maybeNotifyNewMail` 이다(`notifyNewMailFor` 아님). 거기서 걸러야
  소리(`playAlarmSound`)까지 같이 막히고, **`notifiedIds` 에 알림 대상만 기록**된다 —
  걸러진 메일까지 기록하면 나중에 그 종류를 되켜도 영영 알림이 안 온다
- ★`setMailBadge()` 가 **인자 없이 목록에서 직접 센다**. 호출부 6곳이 각자 세면
  화면 배지와 트레이 점·Dock 배지가 어긋난다
- ★`alarmSettings` 선언을 **메일 블록 위로 이동**했다. 시작 시 캐시 복원 IIFE 가 필터를 참조하는데
  기존 위치(설정 UI 근처)에 두면 TDZ ReferenceError 가 난다. 다시 내리지 말 것
- ★`draftAlarm` 은 얕은 복사라 `mailKinds` 객체가 공유된다 → 생성·수정 시 모두 복제한다.
  안 하면 [취소]로 닫아도 이미 반영돼 버린다
- 선행 수정: `main.js` 안읽음 조회 실패를 `|| []` 로 삼키지 않고 throw (오염된 판정 위에 필터를 세우면 안 됨)

검증(브라우저 실구동): 분류 4종 · 기본 전부통과 · 알림만끄기 시 목록 유지 · 숨기기 시 배지·트레이까지 제외 ·
미분류 항상 통과 · 걸러진 메일 알림X · 중복알림 없음 · **되켜면 그때 알림 옴** · 소리 동반 차단 ·
저장 전 실설정 불변 · 취소 시 원복 · 초기화 · 440px 두 줄 배치, 가로 넘침 없음 — 전부 통과.

## 2026-08-27 추가 — 설정창 전면 정돈 (A안 11 + B안 6 + 퇴장 모션, v1.0.27 배포됨)

사용자가 "분류 카테고리랑 안 어울린다"고 한 감각의 정체는 **둘**이었다.
① `.seg-btn.toggle`·`.off-btn` 이 마크업에 있는데 **CSS 규칙이 0줄**이라 다중선택 칩과
단일선택 칩이 픽셀 단위로 같았다. ② 구분선이 4개뿐이고 **마지막 구분선 뒤에 필드가 5개** 붙어
'알람 방식'과 '상태 색상'이 같은 무게로 읽혔다(그룹 경계 45px vs 필드 22px = 2.05배).

- **정보구조 5→6그룹**: '받을 메일 종류'→메일 연동 그룹, '상태 색상' 독립, '알람 방식'→그룹라벨 '알람',
  하위 3개는 `.field.demoted`(13px/600). 구분선 22→28px
- **칩 점 마커** `.seg-btn.toggle::before`(6px, 꺼짐 .28 / 켜짐 1) + `.seg-btn` padding 16→14px
- **헤더 고정**: `.modal-head` 를 `.modal-inner` 밖으로 (스크롤해도 [X] 가 안 사라짐) + 재오픈 시 scrollTop=0
- **퇴장 모션** 0.16s: `.show` 를 유지한 채 `.closing` 을 얹고 끝나면 뗀다 + `reducedMotion()` 분기
- 그 외: 켜진 칩 hover 복구 · `.opt-row` hover · 다크 토글 대비 1.42→12.7:1 · '끄기' 3상태화 ·
  피커 스크롤 닫기 · 죽은 CSS 제거 · **[초기화]가 `openMode` 를 날리던 실버그**

### ★적용하다 만든 회귀 3건 — 전부 수정 완료
1. **`closeModal` 을 비동기로 바꾸면서 `.show` 가 160ms 더 남았다.** 그걸 '열림'으로 읽는 곳이
   5군데(`anyModalOpen`·`isEditingNow`·설정 단축키·키캡처·알람 draft) 있어 **ESC와 단축키가 삼켜졌다.**
   키 리피트(~30ms)에서는 손 뗄 때까지 안 풀렸다. → **`modalShown(el)`** 헬퍼로 일원화.
2. **닫자마자 다시 열면 모달이 사라졌다** — 이전 타이머가 새로 붙인 `show` 를 뗐다.
   → **`openModal(id)`** 신설(타이머 취소 + `.closing` 제거), 여는 9곳 전부 경유. 타이머 콜백에도 가드.
3. 흐려 둔 '자동 실행 시 트레이로 시작' 행이 hover 에 반응했다 → `.is-off` 로 제외.
   ⚠ `pointer-events:none` 은 쓰면 안 된다 — 안내 토스트가 떠야 한다.

### 검증
6에이전트 회귀 검증(범위누출·JS배선·다크·좁은폭·기존기능) + 브라우저 실측.
ESC 홀드 10회 · 단축키 연타 · 3연타 열고닫기 · 다른 모달 · 저장/초기화 흐름 전부 통과.
라이트 토글 손잡이는 **한 픽셀도 안 바뀜**(rgb(255,255,255) 유지) 확인.

⚠️ **세로 +110px**(구분선 +48 · 신설 구분선 +35 · '일반' 설명문 +27). 420px 최소 창에서도 스크롤 정상.
⚠️ 백드롭 클릭(`3846`)은 `closeModal` 을 안 거쳐 퇴장 모션을 안 탄다 — 의도인지 확인 필요.
⚠️ `[저장]/[초기화]` 는 여전히 스크롤 영역 안이다(헤더만 고정).

## 2026-08-27 — GAS v54 배포 완료 · 실측 결과

사용자가 `gas/AppsScript-v54.gs` 를 편집기에 붙여넣고 기존 배포를 편집해 새 버전으로 올렸다.
라이브 웹앱을 직접 호출해 검증했다.

### 동기화 속도 — 문제였던 구간이 사라졌다
| `action=read` 연속 호출 | v53 (전) | v54 (후) |
|---|---|---|
| 1회 | 8.9초 | **4.0초** |
| 2회 | 27.2초 | **1.6초** |
| 3회 | **70.9초 · 응답 0바이트(실패)** | **1.7초** |
| 4회 | 3.2초 | **1.9초** |
| 5회 | 19.9초 | **1.3초** |

연속 호출에서 무너지던 것이 없어졌다. 캐시 적중 시 1.3~1.5초인데,
이건 GAS 웹앱 왕복의 바닥값이다(`action=members` 가 1.8~3.5초인 것과 같은 이유).

### 캐시 동작 검증
- 적중 1.3~1.5초 / 미스 2.5~5.0초로 명확히 갈린다
- 22초 대기 후 호출 = 4.9초(미스) → TTL 20초 정상
- **★쓰기 무효화 2회 반복 확인**: 적중 1.3초 → 쓰기 직후 2.7~3.1초(미스) → 다시 1.3초
  (시트를 건드리지 않는 안전한 요청으로 검증. `bustReadCache()` 가 분기 **전에** 돌기 때문에
   보호 거부로 끝나는 요청도 캐시를 버린다)

### 데이터 정합성
7명 전원의 video/mgmt 개수와 nameRow 가 v53 응답과 **완전히 일치**. `memberNames` 동일, `warn` 없음.
마지막 항목 93행 — READ_ROWS 버그(150행 상한)는 아직 안 터진 상태에서 막았다.

### ⚠️ curl 로 GAS 에 POST 할 때
`-X POST` 나 `--post302` 를 주면 **리다이렉트에서도 POST 를 강제해 스크립트에 도달하지 못한다**
(411 또는 "페이지를 찾을 수 없음"). GAS 는 302 에서 GET 으로 바뀌어야 정상이므로
`curl -sL -d "..."` 형태로만 쓸 것. 한글 값은 `--data-urlencode` 를 써야 빈 값으로 안 간다.

**시트 ID 확정:** `…_4xBs…` 가 정본이다(사용자 확인). `.gs:21` 의 `…_8xBs…` 는 오기.

**실기 확인:** Windows 확인 완료(사용자). ⚠️ **맥은 아직 미확인** — 팀원 맥에서 한 번 봐야 한다.
`updateStats` 는 일부러 안 건드렸다 — 팀원 보드를 볼 때 그 사람 통계가 보이는 건 의도된 동작으로 판단했다.

---

# 이전 기록 (2026-08-05)

## 한 줄 요약
앱은 **Windows·macOS 모두 안정 작동 중**. 맥의 장기 미해결 문제 2건이 최근 모두 해결됐다.
다음 작업은 **① 메일 종류별 알림 필터 ② 자동 업데이트 도입** 두 가지 (→ `TODO.md`).

## 버전 현황
| 항목 | 버전 |
|---|---|
| 타이머 앱 | **v1.0.26** (정식, latest) |
| GAS | **v53** |
| 인트라넷 딸깍 | **v1.5** |
| 설명서 | v1.0.26 반영 완료 |

배포: https://minsk999.github.io/teamtimer-releases/
앱 내 업데이트 배너는 GitHub `releases/latest`의 `tag_name`을 비교해 표시하고,
누르면 위 설명서 페이지로 이동한다 (main.js `fetchLatestRelease`, 73행 부근).

## 검증된 환경
| 기기 | 사양 | 상태 |
|---|---|---|
| Windows PC | — | ✅ 정상 |
| 김본희 iMac20,2 | Intel i9 10코어 / 128GB / macOS 14.5 | ✅ 정상 |
| 주지현 iMac18,3 | Intel i5 4코어 / 32GB / macOS 13.7.8 | ✅ 정상 |
| 주지현 Mac16,11 | **M4 Pro** 12코어 / 64GB / macOS 15.7.3 | ✅ 정상 |

---

## 최근 해결된 문제 (재발 방지용 기록)

### ① macOS 실행 즉시 크래시 — 한글 앱 이름 (v1.0.17에서 해결)
**증상:** 인텔맥에서 실행 즉시 `EXC_BREAKPOINT(SIGTRAP)`. `rbx=0xaaaaaaaaaaaaaaaa`,
`CrBrowserMain`, JS 실행 전 사망, 100% 결정적.

**원인:** Electron은 macOS에서 Info.plist의 `CFBundleDisplayName`/`CFBundleName`으로
`app.getName()`을 정하고, 그 값이 데이터 폴더 경로가 된다.
이름이 한글(`작업 타이머`)이면 `~/Library/Application Support/작업 타이머` → 즉시 죽음.

**오답이었던 것들 (다시 시도하지 말 것):** universal 병합 제거, hardenedRuntime,
entitlements 파일, gatekeeperAssess/category, 멀티아치 동시빌드 분리, ASAR 무결성.
전부 배포해서 확인했고 크래시가 1바이트도 안 변했다.

**결정적 진단법:** 정상 실행되던 진단 빌드(AppTest) DMG와 크래시 DMG를 컨테이너로
다운로드 → 7z로 APFS 추출 → Info.plist/퓨즈/asar해시/스냅샷을 바이트 단위 비교.
Electron 버전·퓨즈·키 구성·해시가 전부 동일했고 **차이는 "이름이 전부 ASCII인가" 하나뿐**이었다.

**대가:** macOS Finder/독/메뉴바 표시가 `TeamTimer`. Windows는 `작업 타이머` 유지.

### ② Apple Silicon 악성코드 차단·자동삭제 (v1.0.25에서 해결)
**증상:** M칩 맥에서 설치 후 실행 시 "악성 코드가 차단되고 휴지통으로 이동함" → 앱 자동 삭제.
`xattr -cr`로도 안 풀림. Intel 맥 2대는 정상.

**원인:** 번들에 `_CodeSignature/CodeResources`가 **하나도 없었다**(v1.0.24 DMG 실측).
실행파일 내부 `LC_CODE_SIGNATURE`는 있었지만, 번들 전체 파일 목록·해시를 담은
CodeResources가 없어 macOS가 "변조된 번들"로 판정.
`mac.identity:"-"`만으로는 electron-builder가 번들 서명을 만들지 않았다.

**결정적 단서:** 사용자가 "클로드 코드로 만든 다른 무서명 앱은 애플실리콘에서 잘 된다"고 언급.
→ 정책 문제가 아니라 이 앱 고유 문제라는 뜻. 단일 실행파일은 CodeResources가 불필요하고,
Electron처럼 중첩 번들만 필수라는 점에서 앞뒤가 맞았다.

**해결:** `sign-mac.js`로 안쪽부터 명시적 서명. CI에 검증 단계 추가(누락 시 빌드 실패).
v1.0.26 DMG 실측 결과 CodeResources 9개 정상.

### ③ 동기화 먹통 데드락 (v1.0.24에서 해결)
`doSync()`에서 `syncInFlight=true`로 잠근 뒤 **try 진입 전에 return**하는 경로가 있어
`finally`가 실행되지 않고 잠금이 영구히 남았다. 이후 모든 동기화가 첫 줄에서 차단.
자동 동기화와 수동 클릭이 겹칠 때만 발생해 "아주 가끔"으로 보였다.
→ 중복 방지를 `syncInFlight` 하나로 일원화, 잠금~try 사이에 return 없음을 검증.

### ④ 팀원 명단 자동화 (GAS v52~53 + 앱 v1.0.23 + 딸깍 v1.5)
이전에는 앱·확장·GAS 5곳에 이름이 하드코딩되어 있었다.
지금은 **`🤖자동화` 시트 A열**만 고치면 전부 자동 반영된다 (재빌드·재설치 불필요).

- 좌/우 배치는 업무현황 시트에서 이름 검색으로 자동 판별
- 이모지 제거는 **한글만 남기는 화이트리스트**. 블랙리스트 방식은
  `୨୧:⋆.˚✮박나진✮˚.⋆🩶`(오디아 숫자 U+0B67-68)에서 실패한 전례가 있다
- `detectStartCol`은 **이름 행 형태인 줄에서만** 검색한다.
  제목 열에 작업 제목이 들어있어, 사람 이름이 제목에 포함되면 오탐이 나기 때문 (v53에서 수정)
- 폴백 3단계: 자동화 시트 → 업무현황 자동감지 → 내장 목록

**퇴사·입사 절차:** 업무현황에서 이름 글자만 교체(새 섹션 X) → 자동화 시트 A열 교체
→ 편집기에서 `clearMemberCache()`. 장식 이모지는 그대로 둬도 무방.

---

## 자동 업데이트 — 현재 상태와 제약

**현재:** 자동 업데이트 **없음**. `fetchLatestRelease()`가 GitHub API로 최신 태그를 확인해
앱 안에 "업데이트 있음" 배너만 띄우고, 사용자가 설명서 페이지에서 **수동 재설치**한다.
`electron-updater` 미설치, `auto-update/` 폴더 없음, `latest.yml`/`latest-mac.yml` 미생성.

**Windows:** 무서명 상태로도 electron-updater 자동 업데이트가 가능하다.

**⚠️ macOS 제약 (중요):** 맥 자동 업데이트는 **Apple Developer 코드서명 + 공증이 필요**하다.
현재는 ad-hoc 서명(`identity:"-"`)이라 Squirrel.Mac이 서명 검증에 실패해 동작하지 않는다.
Apple Developer Program은 **연 $99**. 미가입 상태.

→ 즉 **Windows만 먼저 자동 업데이트를 붙이고, macOS는 수동 재설치를 유지**하는 것이
현실적인 1단계다. 사용자도 이 비용 문제를 사내 보고서에 함께 올릴지 검토 중.

---

## 알려진 미해결 / 미측정

**시트 로딩 속도** — 이 세션 초기에 "맥에서 20~30초"라는 보고가 있었으나,
그때는 `build-secrets`가 비어 있던 **진단용 빌드**였다(공휴일 API 키 없음 → 타임아웃 의심).
이후 맥이 몇 주간 크래시 상태여서 **한 번도 재측정되지 않았다.** 이미 해결됐을 수 있다.

측정 후 느리다면 개선 여지는 있다:
- `doRead` 1회당 약 5,100셀 읽음 (getValues 2,250 + getFormulas 2,250 + richText 600)
- **`ensureMembers()`와 `doRead()`가 같은 범위를 중복해서 읽는다** (팀원 캐시가 식었을 때).
  ensureMembers가 읽은 데이터를 doRead에 넘기면 제거 가능 — 부작용 없는 개선
- `doRead`에 30~60초 캐시 추가 가능. 단 **쓰기 동작 시 캐시 무효화 필수**
  (안 하면 "완료 체크했는데 되돌아옴" 같은 혼란 발생)

**성능 개선 후보** (측정 없이 착수하지 말 것)
1. GAS 읽기 캐시 (위)
2. 알람 감지가 렌더러 1초 tick 안에 있어, 창을 숨기면 backgroundThrottling으로 지연될 수 있음
   → main 프로세스 이관 검토
3. tick 내 `tasks.find` O(n²) → Map
4. `render()` 호출 19곳 디바운스

**코드 상태 참고:** 렌더러의 1초 tick은 이미 diff-update(textContent 비교) 방식이고,
강제 레이아웃 읽기는 리사이즈/모드전환 시에만 발생한다. CSS 트랜지션도 transform/opacity
중심에 will-change 절제 사용으로 양호. **디자인·레이아웃·모션은 수정 금지**(사용자 요청).

## 정리 대상 (선택, 무해)
`_mintest/`, `_apptest/` 폴더와 `mintest.yml`/`apptest.yml` 워크플로 —
맥 크래시 진단용이었고 이제 불필요.
